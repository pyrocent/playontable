{
  "dependencies": {
    "sass": "^1.89.2",
    "minify": "^14.0.0"
  },
  "scripts": {
    "build": "npm run build-html && npm build-css && npm run build-js",
    "build-html": "minify --html public/index.html",
    "build-css": "sass public/static/styles/index.sass public/static/styles/index.css",
    "build-js": "for f in public/static/js/*.js; do minify --js \"$f\"; done"
  }
}